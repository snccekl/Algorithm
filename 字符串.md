# 初识字符串
## 与数组和vector的区别
字符串是若干字符组成的有限序列，也可以理解为是一个字符数组，但是很多语言对字符串做了特殊的规定，接下来说C/C++中的字符串。
在C语言中，把一个字符串存入一个数组时，也把结束符 '\0'存入数组，并以此作为该字符串是否结束的标志。
例如这段代码：
```c++
char a[5] = "asd";
for (int i = 0; a[i] != '\0'; i++) {
}
```
在C++中，提供一个string类，string类会提供 size等接口，可以用来判断string类字符串是否结束，就不用'\0'来判断是否结束。
例如这段代码:
```c++
string a = "asd";
for (int i = 0; i < a.size(); i++) {
}
```
至于vector< char > 和 string 的区别
其实在基本操作上没有区别，但是 string提供更多的字符串处理的相关接口，例如string 重载了+，而vector却没有。
所以一般会定义一个string类型来帮助处理字符串

# 库函数
**打基础的时候，不要太迷恋于库函数。**
**如果题目关键的部分直接用库函数就可以解决，建议不要使用库函数。**
**如果库函数仅仅是 解题过程中的一小部分，并且你已经很清楚这个库函数的内部实现原理的话，可以考虑使用库函数。**、
以下列出一些基本的库函数
[[STL#string]]
s.reverse();//字符串翻转
# 相关题目
## [344. 反转字符串 - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-string/)
### 库函数法
调用reverse函数即可
### 双指针法
```c++
void reverseString(vector<char>& s) {
    int l = s.size() - 1;
    for(int i = 0;i<l/2+1;i++){
        swap(s[i], s[l-i]);
    }
}
```
### 补充swap函数
两种实现方法
第一种：交换数值
```c++
int tmp = s[i];
s[i] = s[j];
s[j] = tmp;
```
第二种：位运算
```c++
s[i] ^= s[j];
s[j] ^= s[i];
s[i] ^= s[j];
```

## [541. 反转字符串 II - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-string-ii/)
直觉性的逻辑：每隔2k个字符的前k的字符，写逻辑代码或者搞两个计数器，来统计2k，再统计前k个字符。
其实在遍历字符串的过程中，只要让 i += (2 * k)，i 每次移动 2 * k 就可以了，然后判断是否需要有反转的区间。

**所以当需要固定规律一段一段去处理字符串的时候，要想想在在for循环的表达式上做做文章。**
```c++
for (int i = 0; i < s.size(); i += (2 * k)) {
            // 1. 每隔 2k 个字符的前 k 个字符进行反转
            // 2. 剩余字符小于 2k 但大于或等于 k 个，则反转前 k 个字符
            if (i + k <= s.size()) {
                reverse(s.begin() + i, s.begin() + i + k );
            } else {
                // 3. 剩余字符少于 k 个，则将剩余字符全部反转。
                reverse(s.begin() + i, s.end());
            }
        }
        return s;
```

## [剑指 Offer 05. 替换空格 - 力扣（LeetCode）](https://leetcode.cn/problems/ti-huan-kong-ge-lcof/)
### 直接法
申请新的空间，用for循环遍历
### 扩容法
将原字符串扩充为空格替换后的大小，之后从后往前遍历（减少时间复杂度）。
因为从前往后在每次添加新元素后，都需要将后面的元素后移一格。
以下为代码
```c++
string replaceSpace(string s) {
    int l = s.size();
    int cnt = 0;
    for(int i = 0;i<l;i++){
        if(s[i] == ' '){
            cnt++;
        }
    }
        
    s.resize(l + cnt * 2);
    l1 = s.size();
    for(int i = l1-1,j=l-1;j<i;j--,i--){
        if(s[j] != ' '){
            s[i] = s[j];
        }
        else{
            s[i] = '0';
            s[i-1] = '2';
            s[i-2] = '%';
            i = i - 2;
        }
    }
    return s1;
```

## [151. 反转字符串中的单词 - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-words-in-a-string/)
### 库函数法
split函数切片，定义新字符串，倒序相加

### 附加要求下
总体思路：
1. 先去掉字符串中多余的空格（三种方法，第一种为使用迭代器后用erase函数，注意erase函数本身的复杂度；第二种为双指针法去掉空格；第三种为类比移除数组元素进行去掉空格，需要resize函数重新确定字符串大小）
2. 将字符串整个倒序
3. 使用双指针法对单个单词进行倒序

## [剑指 Offer 58 - II. 左旋转字符串 - 力扣（LeetCode）](https://leetcode.cn/problems/zuo-xuan-zhuan-zi-fu-chuan-lcof/)
### 直接法
申请额外空间存储字字符串

### 附加
为了不使用额外空间，模仿上一道题的翻转
1. 先对前n个进行翻转。
2. 对剩下的进行翻转
3. 整体翻转

## [28. 找出字符串中第一个匹配项的下标 - 力扣（LeetCode）](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/)
### 第一思路
暴力双重循环匹配

### KMP
见后续有关KMP的介绍

## [459. 重复的子字符串 - 力扣（LeetCode）](https://leetcode.cn/problems/repeated-substring-pattern/)
### 暴力
双循环解决

### 移动匹配
观察发现如果该字符串s是由重复的子串构成的，则将两个s合并后去掉开头与结尾（避免得到本来的两个s）里面一定还有s。可以使用库函数以找到子串

### KMP
注意前缀表的含义，则可发现**当一个字符串由重复子串组成的，最长相等前后缀不包含的子串就是最小重复子串。**
进行数学推导可以发现：
**数组长度减去最长相同前后缀的长度相当于是第一个周期的长度，也就是一个周期的长度，如果这个周期可以被整除，就说明整个数组就是这个周期的循环。**

# KMP
## 为什么是“KMP”
因为是由这三位学者发明的：Knuth，Morris和Pratt，所以取了三位学者名字的首字母。所以叫做KMP
## 用处
KMP主要应用在字符串匹配上。
KMP的主要思想是**当出现字符串不匹配时，可以知道一部分之前已经匹配的文本内容，可以利用这些信息避免从头再去做匹配了。**
所以如何记录已经匹配的文本内容，是KMP的重点，也是next数组肩负的重任。
## 关键--前缀表
**前缀表是用来回退的，它记录了模式串与主串(文本串)不匹配的时候，模式串应该从哪里开始重新匹配。**
**记录下标i之前（包括i）的字符串中，有多大长度的相同前缀后缀。（即最长相等前后缀）**

字符串的**前缀是指不包含最后一个字符的所有以第一个字符开头的连续子串**。
**后缀是指不包含第一个字符的所有以最后一个字符结尾的连续子串**。

**正确理解什么是前缀什么是后缀很重要**!

**匹配失败的位置一定是后缀子串的后面，那么找到与其相同的前缀的后面重新匹配就可以了。**
所以前缀表具有告诉我们当前位置匹配失败，跳到之前已经匹配过的地方的能力。
## 构建前缀表
在KMP算法中，构建前缀表一定要实现next数组
代码如下
```cpp
void GetNext(int* next, const string& s){        //确保不会改变s
	int j = 0;
	next[j] = 0;
	for(int i = 1;i<s.size();i++){
		while(j > 0 && s[j] != s[i]){
			j = next[j-1];
		}
		if(s[j] == s[i]){
			j++;
		}
		next[i] = j;
	}
	//注意：i表示后缀末尾，j表示前缀末尾同时也表示最长相等前后缀长度
```
## 代码（实现strStr()）
如下
```cpp
class Solution {
public:
    void getNext(int* next, const string& s) {
        int j = 0;
        next[0] = 0;
        for(int i = 1; i < s.size(); i++) {
            while (j > 0 && s[i] != s[j]) {
                j = next[j - 1];
            }
            if (s[i] == s[j]) {
                j++;
            }
            next[i] = j;
        }
    }
	int strStr(string haystack, string needle) {
		if(needle.size() == 0){
			return 0;
		}
		int next[needle.size()];
        getNext(next, needle);
        int j = 0;
        for(int i = 0.;i < haystack.size();i++){
	        while(j > 0 && haystack[i] != needle[j]){
		        j = next[j-1];
	        }
	        if(haystack[i] == needle[j]){
		        j++;
	        } 
	        if(j == needle.size()){
		        return i - needle.size() + 1;
	        }
        }
        return -1;
    }
}
```
## 总结
KMP算法，关键在于前缀表（即next数组）的构建，至于next数组是否要减一还是右移还是不动，无非是后续的实现不同。